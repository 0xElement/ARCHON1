node_modules/
*.log
*.log.*
event-bus.log*
.gstack/
.claude/
.DS_Store
*.swp
.env
.env.*
!.env.local.example
*.bak
*.bak[0-9]
*.pre-*-backup
quarantine/
.worktrees/
var/
.backups/
.playwright-mcp/
ds-bundle/

# benchmark run outputs (generated)
benchmark/results-*.json

# git bundles (backups)
*.bundle

# release artifact (produced by npm run pack:release, uploaded to GitHub Releases)
archon-release.zip

# operator-local, never commit (per maintainer)
archon-open-source-message.txt
