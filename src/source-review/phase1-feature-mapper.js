// src/source-review/phase1-feature-mapper.js — Autonomous Agent OS canonical path
// (spec RECOMMENDED_REPO_STRUCTURE). Phase 1 feature mapping is implemented in
// src/dispatch/code-review-dispatcher.js (inventories → blueprint → discovery →
// per-feature mapping). This re-exports the dispatcher entry.
'use strict'
module.exports = require('../dispatch/code-review-dispatcher')
