// src/reporting/report-builder.js — Autonomous Agent OS canonical path
// (spec RECOMMENDED_REPO_STRUCTURE). The authoritative report is written by SCRIBE
// (event-bus Phase 4). The deterministic streaming assembler (Block R) is
// src/pipeline/report-stream.js (digest + assembleReport + appendStream).
'use strict'
module.exports = require('../pipeline/report-stream')
