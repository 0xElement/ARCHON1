// src/orchestrator/agent-router.js — Autonomous Agent OS canonical path
// (spec RECOMMENDED_REPO_STRUCTURE). Agent→model routing is the reused
// src/routing/model-router.js (never hardcode model strings — modelRouter.resolve).
'use strict'
module.exports = require('../routing/model-router')
